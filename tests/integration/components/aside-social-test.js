import { module, skip } from 'qunit';
import { setupRenderingTest } from 'ember-qunit';

module('Integration | Component | aside-social', function(hooks) {
  setupRenderingTest(hooks);

  skip('it renders', async function() {
  });
});
